# Ajustes de pnpm. Prisma y argon2 tienen build scripts (postinstall / build nativo) que
# pnpm bloquea por defecto; aquí se aprueban explícitamente.
allowBuilds:
  '@prisma/engines': true
  argon2: true
  prisma: true
