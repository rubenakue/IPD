// calculateFRC — reparto del Fondo de Riesgo Compartido (ahorro/sobrecoste) por agente.
// Función PURA: entradas → salida, sin I/O, sin fechas del sistema, sin azar.
// La implementación llega en la sesión S3 (TDD: este esqueleto deja los tests en rojo).
import type { FrcInput, FrcResult } from '../../types/domain';

export function calculateFRC(_input: FrcInput): FrcResult {
  throw new Error('Not implemented');
}
