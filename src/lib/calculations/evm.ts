// calculateEVM — indicadores de Earned Value Management del proyecto.
// Función PURA: entradas → salida, sin I/O, sin fechas del sistema, sin azar.
// La implementación llega en la sesión S4 (TDD: este esqueleto deja los tests en rojo).
import type { EvmInput, EvmResult } from '../../types/domain';

export function calculateEVM(_input: EvmInput): EvmResult {
  throw new Error('Not implemented');
}
